# SQL Server Configuration
SA_PASSWORD=YourStrong!Passw0rd
DB_NAME=YourDatabaseName

# Optional: Only needed when restoring from backup
BACKUP_FILE=YourBackupFile.bak
DO_RESTORE=false

# Notes:
# - SA_PASSWORD: Must meet SQL Server complexity requirements (uppercase, lowercase, numbers, symbols)
# - DB_NAME: The name you want for your database (defaults to "TestDB" if not specified)
# - BACKUP_FILE: Only required when DO_RESTORE=true (the name of your backup file in the ./backups folder)
# - DO_RESTORE: Set to "true" to restore from backup, "false" or omit for normal startup
# - Copy this file to .env and update with your actual values
